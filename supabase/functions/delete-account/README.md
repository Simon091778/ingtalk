# delete-account

인증된 사용자의 Storage 파일, 서비스 데이터, Supabase Auth 계정을 순서대로 삭제합니다.

```powershell
npx supabase functions deploy delete-account
```

`SUPABASE_URL`과 `SUPABASE_SERVICE_ROLE_KEY`는 Supabase 호스팅 환경에서 기본 제공됩니다. 서비스 역할 키를 앱의 `.env`에 넣지 마세요.
